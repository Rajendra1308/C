#include<stdio.h>

	//changing main in order to input the command line aruguments

int main(int argc,char*argv[]){
printf("Hello, the number of arguments is %d\n",argc); // this will count the ./a.out as an argument
printf("arg 0 is %s\n",argv[0]);
		











}//main
